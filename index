<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تحليل الشخصيات الخمس</title>
    <!-- Font Awesome للايقونات -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
            transition: background-color 0.3s ease, color 0.2s ease, border-color 0.3s ease;
        }

        /* متغيرات الألوان - الوضع النهاري */
        :root {
            --bg-primary: #f0f2f5;
            --bg-secondary: #ffffff;
            --bg-card: #ffffff;
            --text-primary: #1a1a1a;
            --text-secondary: #4a4a4a;
            --accent: #6b4f8b;
            --accent-light: #8f79b0;
            --shadow: 0 10px 30px rgba(0,0,0,0.08);
            --border: #eaeef2;
            --card-hover: #f8f4ff;
        }

        /* الوضع المظلم */
        [data-theme="dark"] {
            --bg-primary: #1a1b1e;
            --bg-secondary: #2c2d33;
            --bg-card: #2c2d33;
            --text-primary: #f0e9ff;
            --text-secondary: #c7c7d1;
            --accent: #b79be4;
            --accent-light: #9f8ac7;
            --shadow: 0 10px 30px rgba(0,0,0,0.5);
            --border: #3f4048;
            --card-hover: #3a3b44;
        }

        body {
            background-color: var(--bg-primary);
            color: var(--text-primary);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            padding: 1.5rem;
            line-height: 1.6;
        }

        .container {
            max-width: 1300px;
            width: 100%;
        }

        /* رأس الموقع */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 3rem;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .title-section h1 {
            font-size: 2.2rem;
            font-weight: 700;
            background: linear-gradient(135deg, var(--accent), #c49ad9);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .title-section p {
            color: var(--text-secondary);
            font-size: 1.1rem;
            margin-top: 0.3rem;
        }

        /* زر التبديل بين الوضعين */
        .theme-toggle {
            background: var(--bg-secondary);
            border: 1px solid var(--border);
            border-radius: 40px;
            padding: 0.5rem 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            cursor: pointer;
            box-shadow: var(--shadow);
        }

        .theme-toggle i {
            font-size: 1.3rem;
            padding: 0.5rem 0.9rem;
            border-radius: 30px;
            transition: all 0.2s;
            color: var(--text-secondary);
        }

        .theme-toggle i.active {
            background: var(--accent);
            color: white;
        }

        /* شبكة البطاقات */
        .cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));
            gap: 2rem;
            margin: 2rem 0 3rem;
        }

        /* بطاقة الشخصية */
        .character-card {
            background: var(--bg-card);
            border-radius: 32px;
            padding: 1.8rem 1.2rem 1.5rem;
            box-shadow: var(--shadow);
            border: 1px solid var(--border);
            cursor: pointer;
            transition: all 0.25s ease;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            backdrop-filter: blur(2px);
        }

        .character-card:hover {
            transform: translateY(-10px);
            background: var(--card-hover);
            border-color: var(--accent-light);
            box-shadow: 0 20px 35px rgba(106, 79, 139, 0.2);
        }

        /* الصورة الكرتونية (دائرة) */
        .avatar {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            background: linear-gradient(145deg, var(--accent-light), #d9c3f0);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1.2rem;
            border: 4px solid var(--bg-primary);
            box-shadow: 0 8px 0 rgba(0,0,0,0.1);
            font-size: 4.5rem;
            font-weight: bold;
            color: white;
            text-shadow: 2px 2px 0 #4a2f6e;
            transition: 0.2s;
        }

        /* رموز الكرتون المميزة لكل شخصية */
        .avatar[data-character="يسرا"] { background: linear-gradient(135deg, #a8e6cf, #56ab91); }
        .avatar[data-character="عمار"] { background: linear-gradient(135deg, #ffd3b6, #d2917a); }
        .avatar[data-character="مصطفى"] { background: linear-gradient(135deg, #b2d9f7, #5f9bc0); }
        .avatar[data-character="إياد"] { background: linear-gradient(135deg, #fbc8d5, #df8fa3); }
        .avatar[data-character="سلمى"] { background: linear-gradient(135deg, #d5b0e6, #a975c9); }

        .character-card h3 {
            font-size: 1.7rem;
            margin-bottom: 0.25rem;
            color: var(--text-primary);
        }

        .age {
            color: var(--text-secondary);
            font-size: 0.95rem;
            margin-bottom: 0.75rem;
            background: var(--bg-primary);
            padding: 0.2rem 1rem;
            border-radius: 50px;
            display: inline-block;
        }

        .quick-tag {
            font-size: 0.9rem;
            background: var(--accent);
            color: white;
            padding: 0.3rem 1rem;
            border-radius: 30px;
            margin-top: 0.5rem;
            opacity: 0.9;
            max-width: 90%;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        /* قسم تفاصيل التحليل (يظهر عند الضغط) */
        .analysis-section {
            margin-top: 2rem;
            background: var(--bg-secondary);
            border-radius: 50px 50px 30px 30px;
            padding: 2.5rem 2rem;
            box-shadow: var(--shadow);
            border: 1px solid var(--border);
            display: none;
        }

        .analysis-section.active {
            display: block;
            animation: fadeSlide 0.4s ease;
        }

        @keyframes fadeSlide {
            from { opacity: 0; transform: translateY(15px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .analysis-header {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }

        .analysis-avatar {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            background: var(--accent);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            font-weight: bold;
            color: white;
            box-shadow: 0 5px 0 #4f3a6b;
        }

        .analysis-title h2 {
            font-size: 2.2rem;
            line-height: 1.2;
        }
        .analysis-title .age-badge {
            background: var(--accent-light);
            color: white;
            padding: 0.25rem 1.2rem;
            border-radius: 30px;
            font-size: 1rem;
            display: inline-block;
        }

        .analysis-content {
            font-size: 1.15rem;
            color: var(--text-primary);
            background: var(--bg-primary);
            padding: 2rem;
            border-radius: 30px;
            border: 1px solid var(--border);
            white-space: pre-line;
            line-height: 1.8;
        }

        .close-btn {
            background: var(--bg-primary);
            border: 1px solid var(--border);
            color: var(--text-secondary);
            padding: 0.7rem 2rem;
            border-radius: 40px;
            font-size: 1rem;
            cursor: pointer;
            margin-top: 1.5rem;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .close-btn:hover {
            background: var(--accent);
            color: white;
        }

        footer {
            text-align: center;
            color: var(--text-secondary);
            margin-top: 4rem;
            font-size: 0.9rem;
        }

        /* Responsive */
        @media (max-width: 650px) {
            .cards-grid { gap: 1rem; }
            .avatar { width: 120px; height: 120px; font-size: 3.5rem; }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- الهيدر مع زر النهار/الليل -->
        <div class="header">
            <div class="title-section">
                <h1>🔮 معمل تحليل الشخصيات</h1>
                <p>الخمسة الأصليين: اضغط على أي شخصية لقراءة تحليلها النفسي</p>
            </div>
            <div class="theme-toggle" id="themeToggle">
                <i class="fas fa-sun active" id="sunIcon"></i>
                <i class="fas fa-moon" id="moonIcon"></i>
            </div>
        </div>

        <!-- شبكة الشخصيات الخمس (صور كرتونية بالحروف) -->
        <div class="cards-grid" id="characterGrid">
            <!-- يسرا -->
            <div class="character-card" data-name="يسرا">
                <div class="avatar" data-character="يسرا">🌸</div>
                <h3>يسرا</h3>
                <span class="age">٣٥ سنة</span>
                <div class="quick-tag">كروشيه + طاقة إيجابية</div>
            </div>
            <!-- عمار -->
            <div class="character-card" data-name="عمار">
                <div class="avatar" data-character="عمار">🚀</div>
                <h3>عمار</h3>
                <span class="age">١٨ سنة</span>
                <div class="quick-tag">ريادي + ذكي اجتماعيًا</div>
            </div>
            <!-- مصطفى -->
            <div class="character-card" data-name="مصطفى">
                <div class="avatar" data-character="مصطفى">🎮</div>
                <h3>مصطفى</h3>
                <span class="age">١٣ سنة</span>
                <div class="quick-tag">هادئ .. كسل برو ماكس</div>
            </div>
            <!-- إياد -->
            <div class="character-card" data-name="إياد">
                <div class="avatar" data-character="إياد">🦸</div>
                <h3>إياد</h3>
                <span class="age">٨ سنوات</span>
                <div class="quick-tag">أيرون مان + دوشة</div>
            </div>
            <!-- سلمى -->
            <div class="character-card" data-name="سلمى">
                <div class="avatar" data-character="سلمى">🎨</div>
                <h3>سلمى</h3>
                <span class="age">١١ سنة</span>
                <div class="quick-tag">عفوية + مصيبة</div>
            </div>
        </div>

        <!-- منطقة عرض التحليل التفصيلي (تظهر عند الضغط) -->
        <div class="analysis-section" id="analysisBox">
            <div class="analysis-header" id="analysisHeader">
                <div class="analysis-avatar" id="analysisAvatar">🌸</div>
                <div class="analysis-title">
                    <h2 id="analysisName">يسرا</h2>
                    <span class="age-badge" id="analysisAge">٣٥ سنة</span>
                </div>
            </div>
            <div class="analysis-content" id="analysisText">
                <!-- هنا يظهر التحليل من JavaScript -->
            </div>
            <button class="close-btn" id="closeAnalysis"><i class="fas fa-times"></i> إغلاق</button>
        </div>

        <footer>
            ⚡ تحليل نفسي احترافي — ٥ شخصيات حقيقية
        </footer>
    </div>

    <script>
        (function() {
            // ---------- بيانات التحليل الكاملة لكل شخصية ----------
            const analysisData = {
                يسرا: {
                    age: "٣٥ سنة",
                    avatar: "🌸",
                    text: `🔮 تحليل شخصية يسرا (٣٥ سنة):

• السمة الأساسية: شخصية بناءة ومتزنة عاطفياً، عملية وموجهة للرعاية الذاتية.
• قبل النوم: تفكيرها عملي (النوم بدري)، مش بتضيع وقت في القلق.
• في الخصوصية: تمارس الكروشيه (إبداع + تأمل) وتسمع دروس، توازن بين العقل والروح.
• عند الرفض: منطقية، بتسمع الأسباب وبتتناقش، وده نضج عاطفي.
• أسوأ عادة: التسويف، لكنه نابع من كثرة التخطيط.
• سر جاذبيتها: الطاقة الإيجابية + إضحاك الأصدقاء + الصبر.
• خلاصة: إنسانة عملية، متصالحة مع نفسها، خفيفة ظل، بس بتستغرق في التخطيط فتحس بتسويف.`
                },
                عمار: {
                    age: "١٨ سنة",
                    avatar: "🚀",
                    text: `🔮 تحليل شخصية عمار (١٨ سنة):

• السمة الأساسية: قيادي، براجماتي، بيفكر باستراتيجية وذكاء اجتماعي لافت.
• قبل النوم: بيفكر في تطوير مشروعه وخططه المستقبلية (طموح جدًا).
• في الخصوصية: بيكلم صحابه بصوت عالي — طاقته بتتجدد بالتفاعل مش بالعزلة.
• عند الرفض: بيستخدم أسلوب "البديل" و"التأطير" عشان ميأثرش فيه الرفض، ده ذكاء تفاوضي.
• أسوأ عادة: التكاسل في بداية التنفيذ (عقدة البدء) مش في العمل نفسه.
• صفته المحببة: الطيبة لكن المفسرة بذكاء (يتعامل مع كل شخص على قد عقله)، ده "دهاء مقبول".
• خلاصة: شخصية استراتيجية، اجتماعي، عنده قدرة على القيادة مبكرًا، يحتاج فقط يتغلب على تأجيل البداية.`
                },
                مصطفى: {
                    age: "١٣ سنة",
                    avatar: "🎮",
                    text: `🔮 تحليل شخصية مصطفى (١٣ سنة):

• السمة الأساسية: عملي، بسيط، واضح، ومباشر. مزاجه مستقر.
• قبل النوم: بيفكر في "هعمل إيه بكرة" — ده تفكير تخطيطي مبكر، بيهتم بالتفاصيل العملية.
• في الخصوصية: بينجز الحاجات المتأخرة أو بيلعب، شخص مش معقد.
• عند الرفض: "بمشي وأسيبه" — يتجنب المواجهة غير المجدية، وكأن عنده اكتفاء ذاتي.
• أسوأ عادة: الكسل برو ماكس (معترف بيه ومبالغ فيه) وده قد يكون فكاهة أو تجنب للمهام المملة.
• صفته: الهدوء — ده فعلاً نادر في مرحلته العمرية.
• خلاصة: ناضج بالنسبة لسنه، بيعرف يختار معاركه، هادئ الطباع، بس محتاج حافز عشان يكسر روتين الكسل.`
                },
                إياد: {
                    age: "٨ سنوات ونصف",
                    avatar: "🦸",
                    text: `🔮 تحليل شخصية إياد (٨ سنوات ونصف):

• السمة الأساسية: خيال واسع، طاقة حركية عالية، طفل عفوي جدًا.
• قبل النوم: بيفكر أنه "أيرون مان" — الخيال البطولي مسيطر، ده مؤشر ثقة وذكاء تصوري.
• في الخصوصية: بيعمل دوشة عشان مش بحب الهدوء — مش بيحب يفضل لوحده، ممكن يكون اجتماعي أو بيهرب من التفكير.
• عند الرفض: قال "هتقرف" — رد فعل طفولي مباشر، لسه مكتسبش المهارات الدبلوماسية.
• أسوأ عادة: الكسل (على طريقته)، لكنه متناقض مع الدوشة.
• صفته: الكرم — دي صفة تعاطفية جميلة في السن ده.
• خلاصة: طفل ذكي، عنده ثقة (أيرون مان)، اجتماعي، كريم، بس لسه بيعبر عن رفضه بعفوية شديدة "هتقرف".`
                },
                سلمى: {
                    age: "١١ سنة",
                    avatar: "🎨",
                    text: `🔮 تحليل شخصية سلمى (١١ سنة):

• السمة الأساسية: عفوية، مرحة، ومشغولة دائمًا.
• قبل النام: بتفكر في اللي جاي (اليوم اللي بعده) — ده تفكير استباقي بسيط.
• في الخصوصية: "بعمل مصيبة" — دي دعابة، بتحب تعمل حاجات غير متوقعة أو فوضى منظمة.
• عند الرفض: "مش هقعد معاه" — واضحة وحاسمة، مش بتضيع وقت في ناس مش متوافقين معاها.
• أسوأ عادة: الكسل (شبه كل أفراد العائلة!)
• صفتها: العفوية — دي بطاقتها التعريفية. عفوية تعني صريحة، غير متكلفة، وخفيفة.
• خلاصة: مراهقة صغيرة واثقة من نفسها، بتعبر عن رفضها ببساطة، روحها حرة (مصيبة = إبداع فوضوي)، عيبها الوحيد الكسل زي إخواتها.`
                }
            };

            // عناصر الصفحة
            const cards = document.querySelectorAll('.character-card');
            const analysisSection = document.getElementById('analysisBox');
            const analysisName = document.getElementById('analysisName');
            const analysisAge = document.getElementById('analysisAge');
            const analysisAvatar = document.getElementById('analysisAvatar');
            const analysisText = document.getElementById('analysisText');
            const closeBtn = document.getElementById('closeAnalysis');

            // تبديل وضع الإضاءة
            const themeToggle = document.getElementById('themeToggle');
            const sunIcon = document.getElementById('sunIcon');
            const moonIcon = document.getElementById('moonIcon');
            const root = document.documentElement;

            // التحقق من وجود تفضيل سابق
            const savedTheme = localStorage.getItem('theme');
            if (savedTheme === 'dark') {
                root.setAttribute('data-theme', 'dark');
                sunIcon.classList.remove('active');
                moonIcon.classList.add('active');
            }

            themeToggle.addEventListener('click', () => {
                if (root.getAttribute('data-theme') === 'dark') {
                    root.removeAttribute('data-theme');
                    sunIcon.classList.add('active');
                    moonIcon.classList.remove('active');
                    localStorage.setItem('theme', 'light');
                } else {
                    root.setAttribute('data-theme', 'dark');
                    sunIcon.classList.remove('active');
                    moonIcon.classList.add('active');
                    localStorage.setItem('theme', 'dark');
                }
            });

            // فتح التحليل عند الضغط على البطاقة
            cards.forEach(card => {
                card.addEventListener('click', (e) => {
                    const name = card.getAttribute('data-name');
                    const data = analysisData[name];
                    if (data) {
                        analysisName.innerText = name;
                        analysisAge.innerText = data.age;
                        analysisAvatar.innerText = data.avatar;
                        analysisText.innerText = data.text;
                        analysisSection.classList.add('active');
                        // تمرير سلس للتحليل
                        analysisSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }
                });
            });

            // إغلاق التحليل
            closeBtn.addEventListener('click', () => {
                analysisSection.classList.remove('active');
            });

            // (اختياري) إغلاق بالضغط على ESC
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape' && analysisSection.classList.contains('active')) {
                    analysisSection.classList.remove('active');
                }
            });

        })();
    </script>
</body>
</html>
